{
  "name": "deovr-server",
  "version": "1.3.0",
  "description": "A folder browser web app with support for deovr next and back in videos",
  "main": "src/index.js",
  "bin": {
    "deovr-server": "src/index.js"
  },
  "scripts": {
    "dev": "nodemon src/index.js server-test",
    "patch-release": "npm version patch && npm publish && git push --follow-tags",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "commander": "^8.3.0",
    "ejs": "^3.1.10",
    "express": "^4.17.1",
    "ffprobe": "^1.1.2",
    "ffprobe-static": "^3.1.0",
    "serve-favicon": "^2.5.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.1.9",
    "supertest": "^6.3.4"
  }
}
